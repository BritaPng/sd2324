package tukano.clients.Shorts;

import tukano.api.User;
import tukano.clients.User.CreateUser;
import tukano.clients.rest.Shorts.RestShortsClient;
import tukano.clients.rest.User.RestUsersClient;

import java.io.IOException;
import java.net.URI;
import java.util.logging.Logger;

public class CreateShort {
/*
    private static Logger Log = Logger.getLogger(CreateUser.class.getName());

    public static void main(String[] args) throws IOException {
        /*
        if( args.length != 3) {
            System.err.println( "Use: java clients.Shorts.CreateShort shortId ownerId blobUrl");
            return;
        }


        String shortId = args[0];
        String ownerId = args[1];
        String blobUrl = args[2];

        var client = new RestShortsClient( URI.create( serverUrl ) );

        var shortt = new Short( userId, password, email, displayName);

        var result = client.( shortt );
        if( result.isOK()  )
            Log.info("Created short:" + result.value() );
        else
            Log.info("Create short failed with error: " + result.error());
    }



         */
}
